# Application dependencies

require:
    - play
    - org.apache.xmlgraphics -> fop 1.0
    - pdfmvc -> pdfmvc

repositories:
  - LocalModules:
        type:       local
        artifact:   ${application.path}/module-src/[module]
        contains:
                    - pdfmvc